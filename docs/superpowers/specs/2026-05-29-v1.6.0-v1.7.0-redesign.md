# v1.6.0 → v1.7.0 重新规划设计

> 日期: 2026-05-29 | 状态: 待审阅 | 下一阶段: 实现计划

## 架构决策

| 决策 | 选项 | 理由 |
|------|------|------|
| 数据库改造 | 渐进式改造（旧表加索引 + 新表规范化） | 与项目渐进迁移哲学一致，风险可控，500 用户 LAN 场景下性能已足够 |
| 好友系统 | 双向确认制（请求→接受/拒绝→互为好友） | 标准 IM 语义，符合企业场景 |
| AI 面板首期 | 搜索 MVP + 对话摘要，抽象 AIService 接口预留扩展 | 服务端搜索已就绪，LLM 集成通过接口解耦，后续可加 Ollama 离线方案 |
| 密码哈希 | 本次迭代迁移至 bcrypt，旧 sha256 自动升级 | 预防数据静态泄露风险，迁移成本低（~50行），开发阶段用户少 |
| CI/CD | 4 层门禁（pre-commit / pre-PR / 版本质量门 / merge gate） | 企业级质量保障，轻量落地不增加协作负担 |

## 版本路线图

```
v1.6.0 (当前) ✅  Phase 1-4 动画完成
    │
v1.6.1  客户端 DRY + 代码质量
v1.6.2  连接状态 & Store 加固
v1.6.3  C++ Server 安全加固 (bcrypt + 速率限制 + 帧校验)
v1.6.4  UI 边界状态全覆盖
v1.6.5  数据库渐进式改造 (索引 + migration 框架 + friendships 表)
v1.6.6  AI 面板 — 搜索 MVP
v1.6.7  AI 面板 — 对话摘要 (LLM 集成 + AIService 抽象)
v1.6.8  好友系统 — 协议 + 数据库设计
v1.6.9  好友系统 — 服务端 + 客户端实现
v1.6.10 预 v1.7.0 质量门
    │
v1.7.0  正式标签 (AI 面板 + 好友系统 + 安全加固)
```

---

## v1.6.1 — ChatArea/GroupChatArea DRY 重构

**目标：** 消除两个聊天组件 ~80% 代码重复（~150 行）。

**重复项清单：**

| 逻辑 | 提取方案 |
|------|----------|
| `scrollToBottom` + `syncNearBottom` + "New messages" 状态 | `hooks/useChatScroll.ts` |
| `handleSend` + `handleInput` + `handleKeyDown` + auto-resize | `hooks/useChatInput.ts` |
| "↓ New messages" 浮动按钮 | `lib/ChatComponents.tsx` → `NewMessagesFAB` |
| `AnimatePresence` 三态切换 (skeleton/empty/messages) | `lib/ChatComponents.tsx` → `ChatContentSwitcher` |
| 消息输入区 (textarea + send button) | `lib/ChatComponents.tsx` → `MessageComposer` |

**任务清单：**

| # | 任务 | 涉及文件 |
|---|------|----------|
| 1 | 新建 `hooks/useChatScroll.ts` | new |
| 2 | 新建 `hooks/useChatInput.ts` | new |
| 3 | 提取 `NewMessagesFAB`, `ChatContentSwitcher`, `MessageComposer` 到 `lib/ChatComponents.tsx` | new |
| 4 | ChatArea 改用提取的 hooks + components | ChatArea.tsx |
| 5 | GroupChatArea 改用提取的 hooks + components | GroupChatArea.tsx |
| 6 | 验证两组件功能无回归 | 手动测试 |

**验收：** `npx tsc --noEmit` 零错误，两聊天面板功能一致，合计减少 ≥120 行。

---

## v1.6.2 — 连接状态 & Store 加固

**目标：** 消除模块级 `reconnectTimer`，加强连接生命周期管理。

**任务清单：**

| # | 任务 | 涉及文件 |
|---|------|----------|
| 1 | `reconnectTimer` 从模块级移入 store state (`number \| null`) | connectionStore.ts |
| 2 | 最大重连次数 5 次，超限停止并设 error | connectionStore.ts |
| 3 | `disconnect()` 清理 `reconnectTimer` | connectionStore.ts |
| 4 | 连接成功重置 `retryCount` 为 0 | connectionStore.ts |
| 5 | ConnectionBar 显示重连次数/状态 | ConnectionBar.tsx |

**验收：** store 内无模块级变量引用，自动重连最多 5 次后停止。

---

## v1.6.3 — C++ Server 安全加固

**目标：** 基础安全措施补齐 + bcrypt 密码迁移。

### bcrypt 密码迁移

- vendor: `vendor/bcrypt.h`（BSD 许可，单头文件）
- `UserRepository::create()` 改用 bcrypt，cost factor=12
- `UserRepository::verifyPassword()` 查 `password_version`，version=0 走 sha256 验证后自动升级
- 用户 Row 新增 `password_version` 字段（默认 0=sha256, 1=bcrypt）
- 现存用户零感知：下次登录时自动完成升级

### 速率限制

- `AsyncSession` 每 session 每秒最多 20 条消息
- 超限断开连接

### 帧大小 + 端口校验

- 帧大小上限 4 MiB → 256 KiB
- 无效帧丢弃+日志，不返回伪 JSON（防止信息泄露）
- `main.cpp` 端口参数校验（拒绝 0、>65535、<1024 警告）

**改动范围：** `vendor/bcrypt.h`(new), `UserRepository.h/cpp`, `AsyncSession.h/cpp`, `FrameCodec.h/cpp`, `main.cpp`, `frame_codec_tests.cpp`

**验收：** `cmake --build` + `ctest` 通过，bcrypt 迁移幂等。

---

## v1.6.4 — UI 边界状态全覆盖

**目标：** 所有组件在 loading / empty / error / disconnected 四种状态有合理 UI。

| # | 任务 | 涉及文件 |
|---|------|----------|
| 1 | 断连状态：发送按钮 disabled + tooltip "Not connected" | ChatArea, GroupChatArea |
| 2 | 断连状态：输入框 disabled + placeholder "Disconnected..." | ChatArea, GroupChatArea |
| 3 | 群聊成员列表空态 | GroupChatArea |
| 4 | 联系人全部离线视觉提示 | ContactList |
| 5 | 群组列表为空引导文案 | ContactList |
| 6 | 消息发送失败重试按钮 | ChatArea, GroupChatArea |

**验收：** 遍历 4 种边界状态 × 6 个组件，每组合有合理 UI。

---

## v1.6.5 — 数据库渐进式改造 ★ 核心基础设施

**目标：** 为现有表添加 SQLite 表达式索引，建立 migration 框架，创建规范化的 friendships 表。

### Database 类新增方法

```cpp
void createIndex(const std::string& table, 
                 const std::string& indexName,
                 const std::string& expression);
bool migrate(int targetVersion);
```

### 表达式索引（对 Repository 层透明）

```sql
-- users
CREATE INDEX idx_users_id ON users(json_extract(data, '$.id'));
CREATE INDEX idx_users_nickname ON users(json_extract(data, '$.nickname'));

-- messages
CREATE INDEX idx_messages_from_id ON messages(json_extract(data, '$.from_id'));
CREATE INDEX idx_messages_to_id ON messages(json_extract(data, '$.to_id'));
CREATE INDEX idx_messages_group_id ON messages(json_extract(data, '$.group_id'));
CREATE INDEX idx_messages_timestamp ON messages(json_extract(data, '$.timestamp'));

-- groups
CREATE INDEX idx_groups_group_id ON groups(json_extract(data, '$.group_id'));

-- group_members
CREATE INDEX idx_gm_user_id ON group_members(json_extract(data, '$.user_id'));
CREATE INDEX idx_gm_group_id ON group_members(json_extract(data, '$.group_id'));
```

### Migration 框架

基于已有的 `schema_version` 表，顺序执行迁移链：
```
version 0 → 1: 添加表达式索引 + 新建 friendships 表
version 1 → 2: (预留)
```

### 规范化 friendships 表

```sql
CREATE TABLE friendships (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    from_uid    INTEGER NOT NULL,
    to_uid      INTEGER NOT NULL,
    status      TEXT NOT NULL CHECK(status IN ('pending','accepted','rejected')),
    request_msg TEXT DEFAULT '',
    created_at  INTEGER NOT NULL,
    updated_at  INTEGER NOT NULL,
    UNIQUE(from_uid, to_uid)
);

CREATE INDEX idx_fs_from ON friendships(from_uid, status);
CREATE INDEX idx_fs_to   ON friendships(to_uid, status);
CREATE INDEX idx_fs_both ON friendships(from_uid, to_uid);
```

**改动：** `Database.h/cpp`, `database_tests.cpp`(new)

**验收：** migrate(1) 幂等，已有 Repository 查询全部通过，索引对上层透明。

---

## v1.6.6 — AI 面板：搜索 MVP

**目标：** Sidebar 对接服务端已有的 AI 消息搜索接口。

### chatStore 新增

```typescript
searchResults: SearchResult[]
isSearching: boolean
searchMessages(query: string)  // TCP → AIRequest{ ai_type:"search" }
```

`handleIncomingMessage` 新增 case: `AIResponse(30)` → 解析 searchPayload

### Sidebar UI

- 搜索输入框（实时搜索，300ms debounce）
- 搜索结果列表：匹配关键词高亮、显示来源（私聊/群聊名称）、时间戳
- 点击结果项跳转到对应消息位置
- 客户端分类过滤：[全部] [私聊] [群聊]
- 三态：loading（骨架脉冲）/ empty（"No results"）/ results

**改动：** `chatStore.ts`, `Sidebar.tsx`, `ChatArea.tsx`（scrollToMessage 方法）, `message_types.ts`

**验收：** 搜索关键词返回匹配结果，点击跳转到正确消息。

---

## v1.6.7 — AI 面板：对话摘要 + AIService 抽象

**目标：** 实现对话摘要功能，建立可扩展的 AIService 插件架构。

### AIService 抽象接口

```
lib/ai/
├── AIService.ts         ← 抽象接口
├── providers/
│   ├── LocalSearch.ts   ← 已有（ai_type="search"）
│   └── ClaudeAPI.ts     ← 新增
├── useAI.ts             ← AI hook（provider 选择 + 状态管理）
└── types.ts             ← AIRequestType, AIProvider config
```

### AIService 接口

```typescript
interface AIService {
  readonly id: string;
  readonly name: string;
  readonly capabilities: AIRequestType[];
  execute(request: AIRequest): Promise<AIResponse>;
  executeStream?(request: AIRequest, onChunk: AIStreamChunkCallback): Promise<void>;
}
```

### ClaudeAPI Provider

- 通过服务端 AIRequest 中转（客户端不直接调 Anthropic API）
- 服务端新增 `ai_type="summarize"` 处理：收集最近 50 条消息 → 调 Claude API → 流式返回 `AIStreamChunk`
- API key 存储：Tauri 端加密（Rust `keyring` crate 或 AES-GCM 本地加密）
- 协议 `AIStreamChunk(31)` 已就绪，天然支持 SSE → frame 转换

### Sidebar 扩展

- 模式切换: [搜索] [摘要] tabs
- 摘要模式: 下拉选择当前对话 → 点击"生成摘要" → 流式显示结果
- AI 设置区: Provider 选择 / Model 选择 / API Key 管理

### 后续可扩展（v1.7.x+）

| 功能 | 说明 |
|------|------|
| translate | 选中消息右键"翻译" |
| chat | 自由对话（带聊天记录上下文） |
| Ollama | 完全离线本地 LLM provider |
| 智能回复 | "用 AI 回复"、"润色"按钮 |

**验收：** 选择对话→生成摘要→流式显示结果，provider 可切换。

---

## v1.6.8 — 好友系统：协议 + 数据库设计

**目标：** 定义好友协议消息类型，设计数据库表，为服务端/客户端实现奠基。

### 协议新增（type 34–42）

```
FriendRequest(34)      → A 向 B 发送好友请求
FriendRequestAck(35)   → 服务端确认收到请求
FriendAccept(36)       → B 接受好友请求
FriendAcceptReturn(37) → 返回好友信息给双方
FriendRemove(38)       → 解除好友关系
FriendRemoveReturn(39) → 确认解除
FriendList(40)         → 请求好友列表
FriendListReturn(41)   → 返回好友列表
FriendOnline(42)       → 好友上线通知
```

### 好友关系状态机

```
A 发送请求 → [pending] → B 接受 → [accepted] → 互为好友
                  ↓ B 拒绝
              [rejected] → 30 天后清理
              
[accepted] → A 或 B 删除 → 行立即删除
```

### 数据库（已在 v1.6.5 创建）

`friendships` 表使用规范化列（非 JSON blob），`FriendRepository` 直接操作 sqlite3：
- `sendRequest()` / `acceptRequest()` / `rejectRequest()` / `removeFriendship()`
- `getFriends(uid)` / `pendingRequests(uid)` / `areFriends(uidA, uidB)`

**改动：** `protocol/protocol_definitions.json`, `message_types.h`, `message_types.ts`, `protocol/schemas/`(regenerate)

**验收：** 协议类型对齐校验通过，DDL 在空库执行成功。

---

## v1.6.9 — 好友系统：服务端 + 客户端实现

**目标：** 实现完整的端到端好友功能链路。

### 服务端

- `FriendRepository` 实现（直接 SQLite，不经过 Database JSON 抽象层）
- `MessageRouter` 新增 4 个 handler：`handleFriendRequest`, `handleFriendAccept`, `handleFriendRemove`, `handleFriendList`
- `broadcastPresence()` 扩展：上线/离线通知仅广播给好友（不再全量广播）
- 私聊权限校验：`handlePrivateMessage` 中可选开启"仅好友可私聊"

### 客户端

| 层面 | 改动 |
|------|------|
| chatStore | 新增 `friends[]`, `friendRequests[]`, `sendFriendRequest()`, `respondToRequest()`, `removeFriend()` |
| handleIncomingMessage | 新增 5 个 case（FriendRequestAck 35 / FriendAcceptReturn 37 / FriendRemoveReturn 39 / FriendListReturn 41 / FriendOnline 42） |
| ContactList | 右键菜单 "Add Friend"；好友列表分组（在线好友 / 离线好友 / 非好友联系人） |
| App.tsx | 好友请求 toast 通知 + pending badge |
| GroupChatArea | 可选：群成员右键 "Add Friend" |

**改动：** `FriendRepository.h/cpp`(new), `MessageRouter.h/cpp`, `chatStore.ts`, `ContactList.tsx`, `App.tsx`, `GroupChatArea.tsx`

**验收：** 端到端：A 发送好友请求 → B 收到通知 → B 接受 → 双方好友列表出现对方 → 好友上线通知。

---

## v1.6.10 — 预 v1.7.0 质量门

**目标：** v1.7.0 最终标签前的全面审计。

| # | 任务 | 方法 |
|---|------|------|
| 1 | 全栈审计 | `codebase-health-audit` 6-pillar |
| 2 | 构建三连 | `npx tsc --noEmit` + `npm run build` + `cargo check` |
| 3 | C++ 构建+测试 | `cmake --build` + `ctest` |
| 4 | 协议对齐 | `node scripts/audit_protocol.mjs` |
| 5 | 多客户端压力 | `node tests/server_multi_client_smoke.mjs` |
| 6 | Tauri 桌面 E2E | login→send→receive→AI search→friend request→theme toggle |
| 7 | React Profiler | 帧率 > 55fps |
| 8 | Bundle 体积 | 与 v1.6.0 基线对比 |
| 9 | CHANGELOG 同步 | 所有 1.6.x 版本变更记录 |

**验收：** 所有指标达标，打 v1.7.0 标签。

---

## CI/CD 门禁流水线

### L1: Pre-commit（秒级，本地）

| 检查项 | 工具 |
|--------|------|
| C++ 格式 | `clang-format --dry-run` |
| TS 格式 | `prettier --check` |
| 协议一致性 | `node scripts/generate_protocol.mjs --check` |
| 无调试代码 | `grep -r "console.log\|TODO\|FIXME" src/` |

### L2: Pre-PR（分钟级）

| 检查项 | 命令 |
|--------|------|
| TS 类型检查 | `npx tsc --noEmit` |
| 客户端构建 | `npm run build` |
| Rust 检查 | `cargo check` |
| C++ 构建 | `cmake --build` |
| C++ 测试 | `ctest` |
| 协议审计 | `node scripts/audit_protocol.mjs` |
| Bundle 体积 | 对比基线 ±5% |

### L3: 版本质量门（每 0.1 版本）

| 检查项 | 方法 |
|--------|------|
| 全栈审计 | codebase-health-audit (P0/P1 = 0) |
| Tauri 冒烟 | E2E 脚本 |
| 压力测试 | server_multi_client_smoke.mjs |
| 性能回归 | React Profiler + 服务端吞吐量 |
| CHANGELOG | 人工核查 |

### L4: Merge Gate（PR 合并前）

L2 全部通过 + Code Review 完成 + 无冲突 + 版本标签对齐。

### 版本 → 门禁映射

| 版本 | 触发门禁 |
|------|----------|
| v1.6.1–v1.6.4 | L1 per-commit, L2 per-version |
| v1.6.5 | L1 + L2 + DB migration 回滚测试 |
| v1.6.6–v1.6.7 | L1 + L2 + AI 响应格式校验 |
| v1.6.8–v1.6.9 | L1 + L2 + 好友状态机测试 |
| v1.6.10 | L1 + L2 + L3 全量 |
| v1.7.0 | L4 Merge Gate |

### 工具链脚本

```
scripts/gates/
├── pre-commit.ps1         # L1
├── pre-pr.ps1             # L2
├── quality-gate.ps1       # L3
└── db-migration-test.ps1  # 数据库迁移回滚
```

---

## 排除项（永久不做）

- 三个 Backgrounds/* 全屏 WebGL/Canvas 组件
- 所有 `@react-three/*` Three.js 组件（~500KB）
- FluidGlass, Lanyard, ModelViewer（3D 模型）
- 路由库（保持条件渲染）
- 后端重写为其他语言（坚持 C++17）

---

## 设计决策记录

| 日期 | 决策 | 选项 | 理由 |
|------|------|------|------|
| 2026-05-29 | 数据库改造策略 | B: 渐进式改造 | 风险可控，匹配项目哲学，500 用户下性能足够 |
| 2026-05-29 | 好友系统模型 | B: 双向确认制 | 标准 IM 语义 |
| 2026-05-29 | AI 面板首期范围 | B: 搜索 + 摘要 | 服务端搜索已就绪，摘要建立可扩展接口 |
| 2026-05-29 | 密码哈希 | A: 本次迭代迁移 bcrypt | 开发阶段用户少迁移成本低，预防数据泄露 |
| 2026-05-29 | CI/CD 门禁 | 4 层流水线 | 企业级质量保障，轻量落地 |
